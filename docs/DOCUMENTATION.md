# Documentation standard

Family contract: Diátaxis + Standard Readme + GitHub community health files.

Canonical CLI / DX / testing: `docs/guides/`. Ownership law: `docs/OWNERSHIP.md`.
Root `docs/CLI.md` and `docs/FLOW.md` are Moved stubs.

Product CLI is `uxcompose` only. `cli.py` is argv; `serve_dev.py` owns
CSS watch + tunnel. Frozen serve verbs: `dev` / `prod` / `restart-channel`.
This package has no `FEATURES.md` / `ONTOLOGY.md` — shape is
`docs/ARCHITECTURE.md`, law is `docs/OWNERSHIP.md`.
Out of scope this round: `ux-app`, `ux-motion-lib`.
