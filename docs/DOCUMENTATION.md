# Documentation standard

Family contract: Diátaxis + Standard Readme + GitHub community health files.

Canonical CLI / DX / testing: `docs/guides/`. Ownership law: `docs/OWNERSHIP.md`.

Product CLI is `uxcompose` only. Out of scope this round: `ux-app`, `ux-motion-lib`.
